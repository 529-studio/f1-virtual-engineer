---
description: Reduce a broad idea or backlog item to the thinnest demoable MVP slice.
---

Invoke the `indie-hacker-slice` skill on this idea: $ARGUMENTS

Produce exactly the seven-section output the skill specifies (User outcome, Why now, Non-goals, Backend change, Frontend/API surface, Smallest verification proof, What gets postponed).

Constraints to respect:

- One backend contract + one visible frontend/API effect + one focused test or manual proof. If you can't fit the slice into that shape, halve it again and present the smaller version.
- No new infrastructure unless strictly required.
- No black-box strategy claims — every recommendation must be explainable.

After presenting the slice, wait for the user to accept or refine before any code is written.
